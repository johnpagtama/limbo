import { BitButtonDirective } from './bit-button.directive';

describe('BitButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BitButtonDirective();
    expect(directive).toBeTruthy();
  });
});
